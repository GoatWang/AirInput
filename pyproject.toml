[project]
name = "airinput"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyaudio>=0.2.14",
    "pynput>=1.7.6",
    "pyperclip>=1.8.2",
    "numpy>=1.24.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
